{
  "name": "cloudflare-site-migrator",
  "version": "0.1.0",
  "description": "Bun/TypeScript tool for copying Cloudflare site resources between accounts.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "check": "bun build src/index.ts --target=bun --outfile=/tmp/cloudflare-asset-migrator-check.js",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {}
}
